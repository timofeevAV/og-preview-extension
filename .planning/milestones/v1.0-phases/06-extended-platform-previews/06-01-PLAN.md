---
phase: 06-extended-platform-previews
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - entrypoints/popup/components/platform/FacebookMobileCard.tsx
  - entrypoints/popup/components/platform/IMessageCard.tsx
  - entrypoints/popup/components/platform/WhatsAppCard.tsx
autonomous: true

must_haves:
  truths:
    - "FacebookMobileCard renders image (1.91:1), domain (uppercase, above title), title (13px semibold, line-clamp-2), description (12px, line-clamp-1) and uses rounded-xl outer border"
    - "IMessageCard renders image (1.91:1), domain (lowercase, above title), title (13px medium, line-clamp-2) — description is NOT rendered"
    - "WhatsAppCard renders image (1.91:1), title (14px medium, line-clamp-2), description (12px, line-clamp-2), domain at BOTTOM (lowercase, not uppercase)"
    - "All three cards accept OgData prop, call resolveDisplayData(), call extractDomain(), handle missing image with a placeholder div"
    - "All three cards import OgData as a type import and use Tailwind CSS tokens (no hardcoded colors)"
  artifacts:
    - path: "entrypoints/popup/components/platform/FacebookMobileCard.tsx"
      provides: "Facebook mobile news feed card component"
      exports: ["FacebookMobileCard"]
    - path: "entrypoints/popup/components/platform/IMessageCard.tsx"
      provides: "Apple iMessage rich link card component"
      exports: ["IMessageCard"]
    - path: "entrypoints/popup/components/platform/WhatsAppCard.tsx"
      provides: "WhatsApp mobile full-width link card component"
      exports: ["WhatsAppCard"]
  key_links:
    - from: "FacebookMobileCard.tsx"
      to: "lib/og-display.ts"
      via: "resolveDisplayData import"
      pattern: "resolveDisplayData\\(ogData\\)"
    - from: "IMessageCard.tsx"
      to: "lib/og-display.ts"
      via: "resolveDisplayData import"
      pattern: "resolveDisplayData\\(ogData\\)"
    - from: "WhatsAppCard.tsx"
      to: "lib/og-display.ts"
      via: "resolveDisplayData import"
      pattern: "resolveDisplayData\\(ogData\\)"
---

<objective>
Create the three new platform card components: FacebookMobileCard, IMessageCard, and WhatsAppCard.

Purpose: Deliver the visual card simulators required by PLATF-03, PLATF-05, PLATF-06. Each card is a standalone React component following the identical pattern established in Phase 4.
Output: Three .tsx files in entrypoints/popup/components/platform/, each exporting a named card component.
</objective>

<execution_context>
@/Users/a.timofeev/.claude/get-shit-done/workflows/execute-plan.md
@/Users/a.timofeev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-extended-platform-previews/06-RESEARCH.md

@entrypoints/popup/components/platform/FacebookCard.tsx
@entrypoints/popup/components/platform/XCard.tsx
@lib/types.ts
@lib/og-display.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create FacebookMobileCard</name>
  <files>entrypoints/popup/components/platform/FacebookMobileCard.tsx</files>
  <action>
    Create a new component at `entrypoints/popup/components/platform/FacebookMobileCard.tsx`.

    Follow the exact same structure as `FacebookCard.tsx` (read it first for reference) with these specific differences to produce a visually distinct mobile variant:

    - Outer wrapper: `rounded-xl` (not `rounded-lg` — more rounded = key visual distinction)
    - Text area: `px-3 py-1.5` (more compact than desktop's `py-2`)
    - Title: `text-[13px]` (not `text-[14px]`) — slightly smaller
    - Description: `line-clamp-1` (not `line-clamp-2`) — one-line cap
    - Domain: `text-[10px] uppercase text-muted-foreground truncate mb-0.5` — same uppercase convention as desktop Facebook

    Image section: `aspect-[1.91/1] bg-muted`. If `image` is truthy: `<img src={image} alt={ogData.imageAlt ?? title ?? ''} className="w-full h-full object-cover" />`. If falsy: `<div className="w-full h-full bg-muted flex items-center justify-center"><span className="text-xs text-muted-foreground">No image</span></div>`.

    Include the local `extractDomain` helper (same 10-line pure function as in FacebookCard.tsx — do not import from a shared module).

    Props pattern: `interface PlatformCardProps { ogData: OgData }` — use `import type { OgData } from '@/lib/types'`.
  </action>
  <verify>Run `npx tsc --noEmit` from the project root — zero TypeScript errors on the new file.</verify>
  <done>File exists at the specified path, exports `FacebookMobileCard`, compiles without errors, visually distinguishable from `FacebookCard` (rounded-xl, 13px title, 1-line description).</done>
</task>

<task type="auto">
  <name>Task 2: Create IMessageCard</name>
  <files>entrypoints/popup/components/platform/IMessageCard.tsx</files>
  <action>
    Create `entrypoints/popup/components/platform/IMessageCard.tsx`.

    Key constraint: iMessage does NOT display `og:description`. Do NOT destructure `description` from `resolveDisplayData`. Only use `title` and `image`.

    Layout spec:
    - Outer wrapper: `w-full border border-border rounded-2xl overflow-hidden` (most rounded of all cards — `rounded-2xl`)
    - Image section: `aspect-[1.91/1] bg-muted` with same conditional img/placeholder pattern
    - Text area: `px-3 py-2` (no background tint — plain surface)
    - Domain (if truthy): `text-[11px] text-muted-foreground truncate mb-0.5` — lowercase, no uppercase, above title
    - Title (if truthy): `text-[13px] font-medium text-foreground line-clamp-2 leading-snug`

    No description rendered at all — this is accurate to iMessage behavior.

    Include `extractDomain` helper inline (same pattern as other cards).

    Add a JSX comment above the component: `{/* iMessage: approximation — Apple does not publish exact pixel specs. Aspect ratio 1.91:1, no description shown. */}`
  </action>
  <verify>Run `npx tsc --noEmit` — zero errors. Confirm no `description` variable in the file.</verify>
  <done>File exists, exports `IMessageCard`, compiles without errors, contains no description rendering, uses `rounded-2xl`.</done>
</task>

<task type="auto">
  <name>Task 3: Create WhatsAppCard</name>
  <files>entrypoints/popup/components/platform/WhatsAppCard.tsx</files>
  <action>
    Create `entrypoints/popup/components/platform/WhatsAppCard.tsx`.

    Key constraint: domain appears at the BOTTOM of the text block (after description), NOT at the top. Do not copy the Facebook structure where domain comes first. Title comes first.

    Layout spec:
    - Outer wrapper: `w-full border border-border rounded-lg overflow-hidden`
    - Image section: `aspect-[1.91/1] bg-muted` with same conditional img/placeholder pattern
    - Text area: `px-3 py-2 bg-muted/30`
    - Title (if truthy): `text-[14px] font-medium text-foreground line-clamp-2 leading-snug`
    - Description (if truthy): `text-[12px] text-muted-foreground line-clamp-2 mt-0.5`
    - Domain (if truthy): `text-[11px] text-muted-foreground/70 truncate mt-1` — at bottom, lowercase, no uppercase

    JSX order in text area: title → description → domain. This is the defining structural difference from Facebook (where it is: domain → title → description).

    Include `extractDomain` helper inline.
  </action>
  <verify>Run `npx tsc --noEmit` — zero errors. Confirm domain `<p>` appears after description `<p>` in the JSX.</verify>
  <done>File exists, exports `WhatsAppCard`, compiles without errors, domain rendered at bottom of text area after title and description.</done>
</task>

</tasks>

<verification>
After all three tasks:
1. `npx tsc --noEmit` passes with zero errors.
2. Three files exist: `entrypoints/popup/components/platform/FacebookMobileCard.tsx`, `IMessageCard.tsx`, `WhatsAppCard.tsx`.
3. Each file exports its named component.
4. No hardcoded hex colors — all styling via Tailwind tokens.
5. IMessageCard has no description rendering.
6. WhatsAppCard domain is in JSX position after title and description.
</verification>

<success_criteria>
- Three platform card files created and TypeScript-clean
- FacebookMobileCard visually distinct from desktop FacebookCard (rounded-xl, 13px title, 1-line description)
- IMessageCard shows no description (accurate to platform behavior)
- WhatsAppCard domain appears at the bottom of the text area (correct per WhatsApp spec)
- All cards follow the { ogData: OgData } prop pattern and use resolveDisplayData()
</success_criteria>

<output>
After completion, create `.planning/phases/06-extended-platform-previews/06-01-SUMMARY.md`
</output>
