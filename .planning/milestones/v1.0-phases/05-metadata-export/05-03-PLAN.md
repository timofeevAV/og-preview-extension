---
phase: 05-metadata-export
plan: "03"
type: execute
wave: 1
depends_on: ["05-02"]
files_modified:
  - entrypoints/popup/components/MetadataTab.tsx
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Clicking 'Copy <meta> tags' briefly shows 'Copied!' on the button, then reverts"
    - "When clipboard write fails, the button briefly shows 'Failed' on the button, then reverts"
    - "Button label behavior matches the existing 'Copy JSON' button pattern"
  artifacts:
    - path: "entrypoints/popup/components/MetadataTab.tsx"
      provides: "Copy meta tags button with state-driven label"
      contains: "copySnippetsState === 'copied' ? 'Copied!'"
  key_links:
    - from: "entrypoints/popup/components/MetadataTab.tsx"
      to: "copySnippetsState"
      via: "ternary expression in button JSX"
      pattern: "copySnippetsState === 'copied'"
---

<objective>
Fix the "Copy meta tags" button so it displays visual feedback after the user clicks it.

Purpose: The `handleCopySnippets` handler and `copySnippetsState` state machine are fully implemented and correct — the state transitions (idle → copied → idle after 2s, or idle → error → idle) happen on every click. The button label is simply hardcoded and never reads the state. One line must change to make the feedback visible.

Output: `entrypoints/popup/components/MetadataTab.tsx` with a state-driven button label that matches the existing "Copy JSON" button pattern at line 125.
</objective>

<execution_context>
@/Users/a.timofeev/.claude/get-shit-done/workflows/execute-plan.md
@/Users/a.timofeev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-metadata-export/05-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire copySnippetsState to button label</name>
  <files>entrypoints/popup/components/MetadataTab.tsx</files>
  <action>
    At line 131, replace the hardcoded string:

      {'Copy <meta> tags'}

    with a state-driven ternary that mirrors the "Copy JSON" button at line 125:

      {copySnippetsState === 'copied' ? 'Copied!' : copySnippetsState === 'error' ? 'Failed' : 'Copy <meta> tags'}

    No other changes. The handler, state declarations, className, and onClick are all correct as-is.
  </action>
  <verify>
    grep -n "copySnippetsState === 'copied'" entrypoints/popup/components/MetadataTab.tsx

    Expected: one match on approximately line 131, confirming the ternary is present.

    Also confirm the file still compiles:
    npx wxt build --browser chrome 2>&1 | tail -5
  </verify>
  <done>
    Line 131 contains the ternary expression reading copySnippetsState. Build succeeds with no TypeScript errors.
  </done>
</task>

</tasks>

<verification>
After the fix, statically verify both copy buttons use the same feedback pattern:

grep -n "copyJsonState\|copySnippetsState" entrypoints/popup/components/MetadataTab.tsx

Expected output shows both state variables referenced in their respective button labels (around lines 125 and 131), with neither hardcoded.
</verification>

<success_criteria>
- The "Copy meta tags" button label is driven by `copySnippetsState` (ternary present in source)
- "Copy JSON" and "Copy meta tags" buttons use the same label pattern
- `npx wxt build --browser chrome` completes without errors
- META-05 gap is closed: visual feedback now matches what the handler already implements
</success_criteria>

<output>
After completion, create `.planning/phases/05-metadata-export/05-03-SUMMARY.md` following the summary template.
</output>
