---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-lock.yaml
  - wxt.config.ts
  - tsconfig.json
  - tailwind.config.ts
  - postcss.config.cjs
  - .gitignore
  - entrypoints/popup/index.html
  - entrypoints/popup/App.tsx
  - entrypoints/popup/main.tsx
  - entrypoints/popup/style.css
  - entrypoints/background.ts
  - entrypoints/content.ts
  - public/icon-16.png
  - public/icon-48.png
  - public/icon-128.png
autonomous: false

must_haves:
  truths:
    - "Extension installs without errors on Chrome, Edge, and Brave"
    - "Clicking the toolbar icon opens a popup showing placeholder content"
    - "Generated manifest uses optional_host_permissions with <all_urls>, not host_permissions"
    - "pnpm dev starts a dev server with HMR for popup changes"
    - "pnpm build produces a loadable production build at .output/chrome-mv3/"
    - "pnpm run typecheck passes with zero errors"
  artifacts:
    - path: "wxt.config.ts"
      provides: "WXT + manifest configuration"
      contains: "optional_host_permissions"
    - path: "entrypoints/popup/App.tsx"
      provides: "Placeholder popup React component"
      min_lines: 10
    - path: "entrypoints/popup/index.html"
      provides: "Popup HTML entry"
      contains: "root"
    - path: "entrypoints/popup/main.tsx"
      provides: "React createRoot mount"
      contains: "createRoot"
    - path: "entrypoints/popup/style.css"
      provides: "Tailwind CSS directives"
      contains: "@tailwind"
    - path: "entrypoints/background.ts"
      provides: "Service worker placeholder"
      contains: "defineBackground"
    - path: "entrypoints/content.ts"
      provides: "Content script placeholder"
      contains: "defineContentScript"
    - path: "tailwind.config.ts"
      provides: "Tailwind v3 content paths"
      contains: "entrypoints/**"
    - path: "postcss.config.cjs"
      provides: "PostCSS pipeline for Tailwind"
      contains: "tailwindcss"
    - path: "tsconfig.json"
      provides: "TypeScript config extending WXT generated config"
      contains: ".wxt/tsconfig.json"
    - path: "public/icon-16.png"
      provides: "16x16 toolbar icon"
    - path: "public/icon-48.png"
      provides: "48x48 extensions page icon"
    - path: "public/icon-128.png"
      provides: "128x128 CWS listing icon"
  key_links:
    - from: "wxt.config.ts"
      to: ".output/chrome-mv3/manifest.json"
      via: "WXT manifest generation"
      pattern: "optional_host_permissions.*<all_urls>"
    - from: "entrypoints/popup/index.html"
      to: "entrypoints/popup/main.tsx"
      via: "script module import"
      pattern: "src=.*main\\.tsx"
    - from: "entrypoints/popup/main.tsx"
      to: "entrypoints/popup/App.tsx"
      via: "React createRoot render"
      pattern: "createRoot.*render.*App"
    - from: "entrypoints/popup/style.css"
      to: "tailwind.config.ts"
      via: "PostCSS Tailwind pipeline"
      pattern: "@tailwind base"
---

<objective>
Scaffold the complete WXT extension project with React 19, TypeScript, Tailwind CSS v3, and MV3 manifest configured with optional_host_permissions. Create all entrypoints (popup, background service worker, content script) as working placeholders, generate placeholder icons, and verify the extension builds and installs.

Purpose: Establish the working extension skeleton that all later phases build upon. A broken foundation blocks everything.
Output: A fully buildable WXT project that installs in Chrome/Edge/Brave with a placeholder popup.
</objective>

<execution_context>
@/Users/a.timofeev/.claude/get-shit-done/workflows/execute-plan.md
@/Users/a.timofeev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold WXT project with all dependencies, configs, and entrypoints</name>
  <files>
    package.json
    pnpm-lock.yaml
    wxt.config.ts
    tsconfig.json
    tailwind.config.ts
    postcss.config.cjs
    .gitignore
    entrypoints/popup/index.html
    entrypoints/popup/App.tsx
    entrypoints/popup/main.tsx
    entrypoints/popup/style.css
    entrypoints/background.ts
    entrypoints/content.ts
    public/icon-16.png
    public/icon-48.png
    public/icon-128.png
  </files>
  <action>
    Initialize the WXT project and create all files. Follow the research exactly.

    **Step 1: Initialize WXT project**
    Run `pnpm dlx wxt@latest init` with the React template in the project root directory (the repo already exists at `/Users/a.timofeev/Documents/other/og-preview-extension/`). Since the directory already exists with .git and .planning, initialize WXT in a temp directory, then move the generated files into the project root. Alternatively, create the files manually using the research code examples -- this is more reliable than fighting the WXT init interactive prompts.

    Recommended approach: Create files manually from the research code examples. This avoids interactive prompts and ensures exact control.

    **Step 2: Create package.json**
    Use these scripts (from research):
    ```json
    {
      "name": "og-preview-extension",
      "description": "Preview Open Graph cards for any webpage or link",
      "private": true,
      "version": "0.1.0",
      "type": "module",
      "scripts": {
        "dev": "wxt",
        "dev:edge": "wxt -b edge",
        "build": "wxt build",
        "build:edge": "wxt build -b edge",
        "zip": "wxt zip",
        "zip:edge": "wxt zip -b edge",
        "postinstall": "wxt prepare",
        "typecheck": "tsc --noEmit"
      }
    }
    ```
    Then install dependencies:
    - `pnpm add -D wxt @wxt-dev/module-react typescript @types/react @types/react-dom tailwindcss@3 postcss autoprefixer`
    - `pnpm add react react-dom clsx`
    - Run `pnpm install` and `pnpm run postinstall` (wxt prepare) to generate .wxt/ types

    **Step 3: Create wxt.config.ts** (from research -- exact code)
    ```typescript
    import { defineConfig } from 'wxt';
    export default defineConfig({
      modules: ['@wxt-dev/module-react'],
      manifest: {
        name: 'OG Preview',
        description: 'Preview Open Graph cards for any webpage or link',
        version: '0.1.0',
        permissions: ['activeTab', 'storage'],
        optional_host_permissions: ['<all_urls>'],
      },
    });
    ```
    CRITICAL: Use `optional_host_permissions`, NOT `host_permissions`. This is a locked decision.

    **Step 4: Create tsconfig.json** (from research)
    ```jsonc
    {
      "extends": "./.wxt/tsconfig.json",
      "compilerOptions": {
        "strict": true,
        "noUncheckedIndexedAccess": true,
        "jsx": "react-jsx"
      }
    }
    ```

    **Step 5: Create tailwind.config.ts** (from research)
    Content paths: `./entrypoints/**/*.{html,ts,tsx}`, `./components/**/*.{ts,tsx}`

    **Step 6: Create postcss.config.cjs** (from research)
    Plugins: tailwindcss, autoprefixer

    **Step 7: Create popup entrypoint** (4 files from research)
    - `entrypoints/popup/index.html` -- HTML with root div and main.tsx script
    - `entrypoints/popup/main.tsx` -- ReactDOM.createRoot mount
    - `entrypoints/popup/App.tsx` -- Placeholder component with w-[350px] min-h-[200px], shows icon, title "OG Preview", and "Platform previews coming soon" message
    - `entrypoints/popup/style.css` -- @tailwind base/components/utilities directives

    **Step 8: Create background.ts** (from research)
    ```typescript
    export default defineBackground(() => {
      console.log('OG Preview service worker initialized', {
        id: chrome.runtime.id,
        version: chrome.runtime.getManifest().version,
      });
    });
    ```

    **Step 9: Create content.ts** (from research)
    ```typescript
    export default defineContentScript({
      matches: ['<all_urls>'],
      runAt: 'document_idle',
      main(ctx) {
        console.log('OG Preview content script loaded on', window.location.href);
      },
    });
    ```

    **Step 10: Generate placeholder extension icons**
    Create simple colored PNG icons at public/icon-16.png, public/icon-48.png, public/icon-128.png. Use a Node.js script or download a simple placeholder. The simplest approach: write a tiny script that creates solid-color PNG files using raw PNG binary data (no dependencies needed). Alternatively, use ImageMagick if available (`convert -size 128x128 xc:#6366f1 public/icon-128.png`). The icons should be recognizable -- a solid indigo (#6366f1) square is fine for Phase 1.

    **Step 11: Update .gitignore**
    Add WXT-specific entries: `.wxt/`, `.output/`, `node_modules/`, `web-ext.config.ts` (machine-specific).

    **Step 12: Run wxt prepare**
    Execute `pnpm run postinstall` to generate `.wxt/` directory with type declarations. Verify no TypeScript errors after preparation.
  </action>
  <verify>
    Run all of the following and confirm zero errors:
    1. `pnpm run typecheck` -- TypeScript compilation succeeds
    2. `pnpm build` -- Production build succeeds, creates .output/chrome-mv3/
    3. Inspect `.output/chrome-mv3/manifest.json` and confirm:
       - `optional_host_permissions` contains `<all_urls>`
       - There is NO `host_permissions` key
       - `permissions` contains `activeTab` and `storage`
       - `action.default_popup` is set (popup registered)
       - `background.service_worker` is set
       - `content_scripts` array is present with `matches: ["<all_urls>"]`
       - `icons` object has entries for 16, 48, 128
    4. Confirm icon files exist: `ls public/icon-*.png` shows 3 files
  </verify>
  <done>
    All source files exist per the project structure. `pnpm run typecheck` passes. `pnpm build` produces .output/chrome-mv3/ with a valid manifest containing optional_host_permissions and all entrypoints registered.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify extension installs and popup opens in target browsers</name>
  <files>
    .output/chrome-mv3/manifest.json
  </files>
  <action>
    Present the user with verification steps for the production build. The build was completed in Task 1.

    <what-built>
    Complete WXT extension skeleton with:
    - MV3 manifest configured with optional_host_permissions
    - Placeholder popup (React + Tailwind) showing "OG Preview" title and placeholder text
    - Background service worker (placeholder)
    - Content script (placeholder)
    - Placeholder icons (indigo squares)
    - HMR-enabled dev server
    </what-built>

    <how-to-verify>
    **Production build verification (all three browsers):**

    The production build is at `.output/chrome-mv3/` (already built by Task 1).

    1. **Chrome:** Open `chrome://extensions` -> Enable "Developer mode" -> Click "Load unpacked" -> Select the `.output/chrome-mv3/` folder inside the project
       - Confirm: Extension appears with "OG Preview" name and icon, no errors
       - Click the OG Preview icon in toolbar -> Confirm popup opens showing "OG Preview" title and placeholder message
       - Check for console errors: Right-click popup -> Inspect -> Console tab (should be clean)

    2. **Edge:** Open `edge://extensions` -> Enable "Developer mode" -> "Load unpacked" -> Same `.output/chrome-mv3/` folder
       - Confirm same behavior as Chrome

    3. **Brave:** Open `brave://extensions` -> Enable "Developer mode" -> "Load unpacked" -> Same `.output/chrome-mv3/` folder
       - Confirm same behavior as Chrome

    **Dev server verification (Chrome only is fine):**

    4. Run `pnpm dev` in the project directory
       - Confirm: Browser opens automatically with extension loaded
       - Click popup icon -> Confirm popup shows
       - Edit `entrypoints/popup/App.tsx` (e.g., change the title text) -> Confirm change appears in popup without manually reloading (HMR working)
       - Stop dev server with Ctrl+C

    **What to look for:**
    - Extension loads without error badges or warning icons
    - Popup opens with proper styling (not unstyled HTML)
    - Popup has the indigo icon, "OG Preview" title, and placeholder text
    - No console errors in popup or background service worker
    </how-to-verify>

    <resume-signal>Type "approved" if all browsers pass, or describe any issues encountered</resume-signal>
  </action>
  <verify>User confirms extension installs and popup opens correctly in Chrome, Edge, and Brave. User confirms HMR works during development.</verify>
  <done>Extension verified working across all three target browsers. Popup opens with styled placeholder content. Dev server HMR confirmed functional.</done>
</task>

</tasks>

<verification>
Phase 1 is complete when:
1. The generated manifest at `.output/chrome-mv3/manifest.json` contains `optional_host_permissions: ["<all_urls>"]` and does NOT contain `host_permissions`
2. `pnpm run typecheck` exits with code 0
3. `pnpm build` exits with code 0
4. Extension loads without errors in Chrome, Edge, and Brave
5. Popup opens when clicking the toolbar icon in all three browsers
6. `pnpm dev` starts with HMR working for popup changes
</verification>

<success_criteria>
- Extension skeleton is fully functional across Chrome, Edge, and Brave
- All config files follow research recommendations exactly (WXT ^0.20.17, React 19, Tailwind v3, TypeScript strict)
- Manifest uses optional_host_permissions per locked decision
- Development workflow is ready: dev server with HMR, production build, typecheck
- Foundation is ready for Phase 2 (Data Pipeline) to add functionality to background.ts and content.ts
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
